/*
 * @Description: 业务层
 * @Usage: 接收处理路由参数
 * @Author: xxx
 * @Date: 2020-12-22 15:24:25
 * @LastEditTime: 2024-01-04 06:13:12
 */

import { KoattyContext, Controller, Autowired, RequestMapping, RequestBody } from 'koatty';
import { App } from '_SUB_PATH/App';
// import { ITestService } from '_SUB_PATH/service/ITestService';

@Controller('_NEW')
export class _CLASS_NAME {
  app: App;
  ctx: KoattyContext;

  // @Autowired()
  // protected TestService: ITestService;
  
  /**
   * constructor
   *
   */
  constructor(ctx: KoattyContext) {
    this.ctx = ctx;
  }

  /**
   * index 接口
   *
   * @param {string} data
   * @returns
   * @memberof _CLASS_NAME
   */
  @RequestMapping('/')
  index(@RequestBody() body: string): Promise<any> {
    return this.ok('Hi Koatty');
  }

}